import '@testing-library/jest-dom';
// import { render } from '@testing-library/react';
// import App from '../renderer/App';

describe('App', () => {
  it('dummy test', () => {
    expect(1 + 2).toBe(3);
  });

  /* TODO Added by Carsten on 2024-12-04. Currently, module @cornerstonejs makes trouble, so we have to disable test in the meanwhile
  it('should render', () => {
    expect(render(<App />)).toBeTruthy();
  });

   */
});
